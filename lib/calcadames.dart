import 'package:flutter/material.dart';

class CalcadamesScreen extends StatelessWidget {
  const CalcadamesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
